# vite-plugin-monitor

> Track every user interaction in your Vite app : clicks, HTTP calls, JS errors, console output and client-side navigations : with structured JSONL file logging and a built-in real-time dashboard.

Inspired by **log4j / Winston** for structured logging and **Sentry / Instana** for the monitoring experience.

---
